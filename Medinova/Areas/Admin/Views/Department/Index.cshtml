@model IEnumerable<Medinova.Models.Department>
@{
    ViewBag.Title = "Departmanlar";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="space-y-6">
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-2xl font-bold text-slate-900 dark:text-white">Departman Yönetimi</h1>
            <p class="text-slate-500 dark:text-slate-400 mt-1">Tıbbi departmanları yönetin</p>
        </div>
        <a href="@Url.Action("Create", "Department", new { area = "Admin" })"
           class="bg-primary hover:bg-primary/90 text-white px-4 py-2.5 rounded-lg text-sm font-semibold flex items-center gap-2 transition-colors">
            <span class="material-symbols-outlined !text-lg">add</span>
            Yeni Departman
        </a>
    </div>

    @if (Model != null && Model.Any())
    {
        <div class="bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 overflow-hidden">
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-slate-200 dark:divide-slate-700">
                    <thead class="bg-slate-50 dark:bg-slate-900/50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase">Departman</th>
                            <th class="px-6 py-3 text-right text-xs font-semibold text-slate-500 dark:text-slate-400 uppercase">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-slate-200 dark:divide-slate-700">
                        @foreach (var department in Model)
                        {
                            <tr class="hover:bg-slate-50 dark:hover:bg-slate-900/40">
                                <td class="px-6 py-4 text-sm font-medium text-slate-900 dark:text-white">@department.Name</td>
                                <td class="px-6 py-4 text-right">
                                    <div class="flex items-center justify-end gap-2">
                                        <a href="@Url.Action("Update", "Department", new { area = "Admin", id = department.DepartmentId })"
                                           class="p-1.5 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-lg hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors"
                                           title="Düzenle">
                                            <span class="material-symbols-outlined !text-lg">edit</span>
                                        </a>
                                        <form method="post" action="@Url.Action("Delete", "Department", new { area = "Admin", id = department.DepartmentId })"
                                              onsubmit="return confirm('Bu departmanı silmek istediğinize emin misiniz?');" class="inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit"
                                                    class="p-1.5 bg-red-100 dark:bg-red-900/30 text-red-600 dark:text-red-400 rounded-lg hover:bg-red-200 dark:hover:bg-red-900/50 transition-colors"
                                                    title="Sil">
                                                <span class="material-symbols-outlined !text-lg">delete</span>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    }
    else
    {
        <div class="bg-white dark:bg-slate-800 rounded-xl border border-slate-200 dark:border-slate-700 p-12 text-center">
            <span class="material-symbols-outlined !text-5xl text-slate-300 dark:text-slate-600 mb-4">domain</span>
            <h3 class="text-lg font-semibold text-slate-700 dark:text-slate-300 mb-2">Departman bulunamadı</h3>
            <p class="text-slate-500 dark:text-slate-400 mb-6">İlk departmanı ekleyerek başlayın.</p>
            <a href="@Url.Action("Create", "Department", new { area = "Admin" })"
               class="inline-flex items-center gap-2 bg-primary hover:bg-primary/90 text-white px-5 py-2.5 rounded-lg text-sm font-semibold transition-colors">
                <span class="material-symbols-outlined !text-lg">add</span>
                Departman Ekle
            </a>
        </div>
    }
</div>