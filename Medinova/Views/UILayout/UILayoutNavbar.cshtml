<div class="container-fluid sticky-top bg-white shadow-sm">
    <div class="container">
        <nav class="navbar navbar-expand-lg bg-white navbar-light py-3 py-lg-0">
            <a href="index.html" class="navbar-brand">
                <h1 class="m-0 text-uppercase text-primary"><i class="fa fa-clinic-medical me-2"></i>Medinova</h1>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav ms-auto py-0">
                    <a href="@Url.Action("Index", "Default")" class="nav-item nav-link active">Anasayfa</a>
                    <a href="@Url.Action("Index", "Default")#hakkimizda" class="nav-item nav-link">Hakkımızda</a>
                    <a href="@Url.Action("Index", "Default")#hizmetler" class="nav-item nav-link">Hizmetler</a>
                    <a href="@Url.Action("Index", "Default")#paketler" class="nav-item nav-link">Paketler</a>
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle" data-bs-toggle="dropdown">Sayfalar</a>
                        <div class="dropdown-menu m-0">
                            <a href="@Url.Action("Index", "Default")#ekip" class="dropdown-item">Doktorlarımız</a>
                            <a href="@Url.Action("Index", "Default")#randevu" class="dropdown-item">Randevu</a>
                            <a href="@Url.Action("Index", "Default")#arama" class="dropdown-item">Doktor Ara</a>
                            <a href="@Url.Action("Index", "Default")#yorumlar" class="dropdown-item">Hasta Yorumları</a>
                        </div>
                    </div>
                    <a href="@Url.Action("Login", "Account")" class="nav-item nav-link">Giriş Yap</a>
                    <a href="@Url.Action("Index", "Default")#iletisim" class="nav-item nav-link">İletişim</a>
                </div>
            </div>
        </nav>
    </div>
</div>