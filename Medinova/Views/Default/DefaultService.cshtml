@model Medinova.Dtos.Defaults.DefaultServiceDto

<div class="container-fluid py-5" id="hizmetler">
    <div class="container">

        <div class="text-center mx-auto mb-5" style="max-width: 500px;">
            <h5 class="d-inline-block text-primary text-uppercase border-bottom border-5">
                Hizmetler
            </h5>
            <h1 class="display-4">Üstün Sağlık Hizmetleri</h1>
        </div>

        <div class="row g-5">

            @if (Model?.Services == null || !Model.Services.Any())
            {
                <div class="col-12 text-center">
                    <p>Henüz içerik yok.</p>
                </div>
            }
            else
            {
                foreach (var service in Model.Services)
                {
                    // 🔥 FA5 / FA4 → FA6 normalize
                    var iconClass = (service.Icon ?? "")
                        .Replace("fa fa-", "fa-solid fa-")
                        .Replace("fas fa-", "fa-solid fa-")
                        .Replace("far fa-", "fa-regular fa-")
                        .Replace("fal fa-", "fa-light fa-");

                    if (string.IsNullOrWhiteSpace(iconClass))
                    {
                        iconClass = "fa-solid fa-stethoscope";
                    }

                    <div class="col-lg-4 col-md-6">
                        <div class="service-item bg-light rounded d-flex flex-column
                                     align-items-center justify-content-center text-center shadow-sm">

                            <div class="service-icon mb-4">
                                <i class="@iconClass text-white fa-2x"></i>
                            </div>

                            <h4 class="mb-3">@service.Title</h4>
                            <p class="m-0 text-muted">@service.Description</p>

                            <a class="btn btn-lg btn-primary rounded-pill" href="#!">
                                <i class="fa-solid fa-arrow-right"></i>
                            </a>
                        </div>
                    </div>
                }
            }

        </div>

    </div>
</div>
